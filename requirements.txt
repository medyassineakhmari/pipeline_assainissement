# API
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
sse-starlette==2.1.3

# Tâches asynchrones
celery==5.4.0
redis==5.2.0

# Utilitaires
pydantic==2.10.2
python-dotenv==1.0.1

# Note : les dépendances ML/SIG (geopandas, sklearn, etc.)
# sont déjà présentes dans l'environnement conda rapids_gpu
# et ne sont PAS réinstallées ici.
rasterio